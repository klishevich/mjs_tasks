// function isInteger(x) {
//     if (Math.round(x) === x) {
//         return true;
//     }
//     return false;
// }

// function isInteger(x) { return (x ^ 0) === x; } 

// val = 122.1;
// console.log(isInteger(val));

// Number.isInteger()

console.log('sdsd' ^ 0);